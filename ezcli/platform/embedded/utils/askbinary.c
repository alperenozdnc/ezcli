#include <ezcli/askbinary.h>

bool askbinary(char *msg, char **yes_aliases, char **no_aliases,
               bool is_no_inclusive) {
    (void)msg;
    (void)yes_aliases;
    (void)no_aliases;
    (void)is_no_inclusive;

    return 0;
}
